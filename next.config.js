/** @type {import('next').NextConfig} */
const nextConfig = {
  // Netlify handles Next.js natively — no static export needed.
  // If you ever want a pure static export for GitHub Pages, uncomment:
  // output: 'export',
};

module.exports = nextConfig;